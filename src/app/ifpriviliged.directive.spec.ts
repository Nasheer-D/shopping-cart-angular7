import { IfprivilegedDirective } from './ifpriviliged.directive';

describe('IfprivilegedDirective', () => {
  it('should create an instance', () => {
    const directive = new IfprivilegedDirective();
    expect(directive).toBeTruthy();
  });
});
